algoritmo "semnome"

var

       v: vetor[1..10] de inteiro
       c: inteiro

inicio

                Para c <- 1 ate 10 faca
                   v[c] <- (c*5)
                   EscrevaL (v[c])
                FimPara

fimalgoritmo
