algoritmo "semnome"

var

     x1, x2: real

inicio

     Escreval ("Quantos reais (R$) você tem na carteira?")
     Leia (x1)
     x2 <- (x1*3.45)
     Escreval ("Com a quantidade de reais que possui, você pode comprar", x2," dólares.")


fimalgoritmo
